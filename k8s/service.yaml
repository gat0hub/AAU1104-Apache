apiVersion: v1
kind: Service
metadata:
  name: apache-demo
  labels:
    app: apache-demo
spec:
  type: NodePort
  selector:
    app: apache-demo
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30100
